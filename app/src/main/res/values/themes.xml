<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ITStepik" parent="android:Theme.Material.Light.NoActionBar" >
        <item name="colorPrimary">@color/items</item>
        <item name="colorBackgroundFloating">@color/background</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textColorLink">@color/check</item>
    </style>

    <style name="CustomRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/custom_rating</item>
        <item name="android:minHeight">32dp</item> <!-- Убедитесь, что высота меньше ширины -->
        <item name="android:maxHeight">32dp</item>
    </style>
</resources>